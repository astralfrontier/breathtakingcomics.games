import { groupAssetsByGroup } from "../assets"
import AssetList from "../AssetList"
import SupportDetail from "./SupportDetail"
import support from "./SupportData"
export const supportByGroup = groupAssetsByGroup(support, ["organizations", "settings"])

# Supporting Cast and World

<AssetList prefix="support" assets={supportByGroup['']} />

# Organizations

<AssetList prefix="support" assets={supportByGroup['organizations']} view={SupportDetail}  />

# Settings

<AssetList prefix="support" assets={supportByGroup['settings']} view={SupportDetail} />